<% content_for :head do %>
    修改信息-微信通联
<% end %>

<h2 class="visible-phone" style="font-size:20px;line-height:20px;">
  修改信息
</h2>

<h2 class="hidden-phone">修改信息</h2>

<div>
  <%= form_for(@sys_group) do |f| %>
    <div>
      <label>名称</label>
      <%= f.text_field(:name)%>
    </div>
    <div>
      <label>联系方式</label>
      <%= f.text_field(:contact_phone)%>
    </div>
    <div >
      <%= link_to "取消", :back, :class => "btn"%>
      <button type="submit" class="btn" >保存</button>
    </div>
  <% end %>
</div>
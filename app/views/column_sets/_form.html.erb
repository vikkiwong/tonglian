<%= form_for(@column_set) do |f| %>
  <% if @column_set.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@column_set.errors.count, "error") %> prohibited this column_set from being saved:</h2>

      <ul>
      <% @column_set.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :id %><br />
    <%= f.number_field :id %>
  </div>
  <div class="field">
    <%= f.label :column_name %><br />
    <%= f.text_field :column_name %>
  </div>
  <div class="field">
    <%= f.label :active %><br />
    <%= f.check_box :active %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
